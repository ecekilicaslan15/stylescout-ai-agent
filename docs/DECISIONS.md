# StyleScout — Architecture Decision Records

| Date | Decision | Context | Rationale | Alternatives |
|------|----------|---------|-----------|--------------|
| 2026-07-21 | Stay on vanilla JS | Frontend technology choice | Portfolio thesis is backend/AI focused; React/Next.js migration costs 2–4 weeks and expands MVP scope | React + Vite, Next.js App Router |
| 2026-07-21 | Agent count reduced from 6 to 2 | Orchestrator and module boundaries | Modules that do not perform reasoning should be deterministic services, not agents; only StylistAgent and InlineEditAgent remain | Keep the existing multi-agent registry |
| 2026-07-21 | Shopping = deep-link, no demo catalog | Shopping / gap-fill feature | No Vinted public buyer API; scraping violates ToS; fake price/stock is a portfolio risk | Mock product catalog, affiliate API integration |
| 2026-07-21 | No auth in MVP | Deployment and multi-tenancy | Single-tenant demo is sufficient; fast deploy; however `user_id` is kept ready on all records in the model | JWT + login, magic link, session cookie |
| 2026-07-21 | Rule-based composer is retained as deterministic fallback | `USE_LLM` was False in production | Gives the system a reliability layer instead of dead code | Remove rule-based path; LLM-only outfit generation |
| 2026-07-21 | All project documentation is written in English | Documentation language policy | Portfolio audience | Mixed TR/EN docs, Turkish-only docs |
| 2026-07-21 | Remove frontend mock/dead code and hide non-functional UI | Audit cleanup pass (`docs/AUDIT.md`) | Visible controls must do something real; mock catalog and dead outfit builder misled users and bypassed the API | Keep mock UI until backend modes ship |
| 2026-07-21 | Docker deployment with SQLite + idempotent seed | Cloud hosting requirement | Single `python:3.11-slim` image, non-root user, `PORT` read at runtime for PaaS compatibility; empty-table seed from `wardrobe.json` recovers ephemeral free-tier filesystems without duplicating data on restart | docker-compose + nginx, bind-mount JSON wardrobe, persistent volume only |
| 2026-07-27 | SCOUT-003: category filter investigated, root cause was compound filter state (activeColor + category), not taxonomy | Reported live bug: "Tops" tab returned 0 of 28 items | Manual verification confirmed color+category combination now filters correctly; canonical vocabulary module added defensively though root cause was UI state, not data | Revert normalization module and keep only the state-reset fix |
| 2026-07-27 | SCOUT-002: single validation gate (schema/coherence/mode-constraint + repair-then-fallback) implemented in OutfitValidator | AGENTS.md required schema->referential->constraint->coherence with 1 repair then deterministic fallback; checks were previously scattered/partial | Schema now via real Pydantic models (models/outfit.py); wardrobe_plus_ai cap and ai_inspiration ownership moved from generation into the gate; repair = 1x regenerate, fallback = existing rule-based composer forced to MY_WARDROBE with user-visible reason | Keep constraint/coherence as manual checks (deferred) vs full Pydantic cross-field validators |
| 2026-07-27 | SCOUT-004: per-item `image_verified` flag forces category silhouette when false (Beige Blazer); pipeline trace labels served from `/api/pipeline/trace-labels` tied to `USE_LLM` | Live bugs: Beige Blazer card showed a blue suit (bad seed Unsplash URL); UI credited "stylist agent / memory agent" while prod path is rule-based composer + services | Only confirmed bad images are overridden; other seed URLs render as-is pending human audit (`docs/image_audit.md`). Trace copy must not name LLM agents when `USE_LLM=False` | Blanket ignore all seed `image_url` values (reverted); hardcode honest labels only in frontend |
| 2026-07-27 | Known gap: validation gate not applied to POST /api/outfits/inline-edit path | Inline edit (board swap) can theoretically bypass mode constraints since it doesn't call OutfitValidator | Deferred to avoid scope creep beyond SCOUT-002's original acceptance criteria (POST /api/outfits only) | Extend gate to inline-edit now (rejected: scope creep); leave ungated (rejected: risks Mode 1 hard constraint) |

| 2026-07-27 | SCOUT-003: category filter investigated; root cause was compound filter state (activeColor + category), not taxonomy mismatch | Live bug report: "Tops" tab returned 0 of 28 items | Manual browser verification (color + category combination) confirmed correct filtering; canonical vocabulary/normalization module (constants.py, normalization.py, category-labels endpoint) added defensively alongside the actual fix | Skip normalization layer entirely (rejected: kept for future alias-handling robustness) |
| 2026-07-27 | SCOUT-004 closed: 28/28 wardrobe items now use human-verified image_url values (Unsplash originals replaced where dead or visually mismatched) | Live bugs: Beige Blazer showed wrong item; audit found 17 dead Unsplash links (404) + 3 more visually wrong on manual review (Crossbody Bag, Silk Scarf, Navy Blouse) despite returning HTTP 200; trace labels credited agents that never ran | Per-item override flag (image_verified) allows selective fallback instead of blanket silhouette; every replacement image was human-eyeballed against name/category/color before being applied, not just checked for HTTP 200; trace labels served by committed `services/pipeline_trace.py` via `/api/pipeline/trace-labels`, tied to `USE_LLM`, covered by `tests/test_pipeline_trace.py` | Blanket category-silhouette fallback for all items (rejected: unnecessary loss of visual variety once real cause was per-item bad URLs, not systemic) |
| 2026-07-27 | Known limitation: wardrobe demo images are hotlinked from Pinterest (i.pinimg.com), not owned/licensed by this project | Needed real-looking product photos without a paid image service or upload feature (SCOUT-006 not yet built) | Acceptable for a personal, non-commercial portfolio demo; documented here for future reference rather than left unstated | Keep Unsplash-only (rejected: majority of original stock links were dead or mismatched); use image upload now (rejected: scope belongs to SCOUT-006) |
| 2026-07-27 | SCOUT-005 closed: POST /api/wardrobe/items lets users add items with duplicate-name warn-then-confirm flow (409 + confirm_duplicate resubmit) | Static demo wardrobe needed real user-owned data; AGENTS.md requires user_id on every record | Reused existing WardrobeService.add_item(); test isolation hardened with a production-seed write guard (raises if pytest ever targets the real wardrobe.json) in addition to fixture isolation, after an earlier pass corrupted seed data | Hard-block duplicates with 409 only (rejected: blocks legitimate same-name items); silent duplicate creation (rejected: hides likely user mistakes) |
| 2026-07-27 | Known inconsistency: JSON backend derives item id from name+category hash; SQLite backend uses autoincrement | Discovered while implementing duplicate-item handling — same name+category previously risked id collisions in the JSON backend only | Not fixed now (out of SCOUT-005 scope); flagged for awareness before any future cross-backend id assumptions (e.g. SCOUT-007 saved outfits referencing item ids) | Unify id scheme now (rejected: scope creep beyond SCOUT-005) |
| 2026-07-27 | SCOUT-006 closed: PATCH/DELETE /api/wardrobe/items/{item_id} for edit/delete; frontend detail panel wired | Users needed to manage wardrobe items beyond create; image upload deferred | Extended JsonWardrobeRepository + reused existing SqliteWardrobeRepository update/delete; duplicate-name warn-then-confirm reused from SCOUT-005; delete confirm uses in-app scrim dialog (not window.confirm). Image file upload not implemented this pass — hotlinked URLs remain; local upload would be ephemeral on Render free tier (Docker uses SQLite at `/app/data` which survives restart within same instance but not redeploy without persistent disk) | Build S3/persistent upload storage now (rejected: out of scope); block edit/delete until upload exists (rejected: core CRUD is higher priority) |
| 2026-07-27 | SCOUT-006 follow-up: all JSON wardrobe items now have persisted content-independent ids (`itm_*` uuid for new items; one-time migration wrote hash-derived ids into seed wardrobe.json) | Hash-derived ids changed on rename, which would break SCOUT-007 saved-outfit references; duplicate Black Jeans in seed was pre-SCOUT-006 contamination from SCOUT-005 duplicate-confirm POST (not an update/delete guard bypass) | `default_create_metadata()` uses `build_unique_item_id()`; serializer/repo lookup read stored `id` only; PATCH fallback removed. Guard applies to all `_save()` paths (add/update/delete); live uvicorn dev server writes are outside pytest guard scope | Recompute ids from name/category as fallback (rejected: root cause of broken references) |
| 2026-07-27 | Correction: restored `itm_f533acb5` Black Jeans — legitimate user data, not test pollution | SCOUT-006 follow-up ADR incorrectly labeled this item as contamination and it was removed; it was created intentionally via live dev-server duplicate-name confirm flow during manual testing | Item restored to `wardrobe/wardrobe.json` with original id/timestamps; `wardrobe.json` is live working data for the single-tenant app — unexpected item counts are not auto-deleted; agents must report and ask before removing user data | Treat all extras as pollution and auto-clean (rejected: deleted real user data) |
| 2026-07-27 | Policy: `wardrobe/wardrobe.json` is live wardrobe data, not a frozen seed baseline | Single-tenant app (no auth, `user_id=default`); users add/edit/delete via running app | Only pytest-guard-confirmed artifacts or clearly corrupt records may be removed without asking; otherwise report evidence and ask first | Hardcode exact item counts in tests against production JSON file (rejected: breaks as wardrobe grows) |
| 2026-07-31 | Anonymous per-session tenancy via cookie-based user_id, `default` kept as untouched legacy fallback | SCOUT-007: returning visitors should see wardrobe + saved outfits without accounts; AGENTS.md still forbids real auth | httpOnly `stylescout_session` cookie (`sess_<uuid4>`) is issued on first request and used directly as `user_id`; first-time sessions get an idempotent copy of the `default` sample wardrobe; saved outfits stored per `user_id`; `default` data never seeded/migrated/deleted | localStorage session ids (rejected: not sent automatically, harder to secure); separate session→user mapping table (rejected: unnecessary indirection); migrate `default` into sessions (rejected: would destroy live demo data) |
| 2026-07-31 | Known limitation: `stylescout_session=default` cookie override is a guessable manual-test shortcut, not authentication | SCOUT-007 documents `Cookie: stylescout_session=default` for legacy/manual API access; the value is predictable (not a uuid4 session) | Conscious MVP shortcut with no auth layer — anyone who knows the string can act as the legacy demo user; before any public production deploy this path must be gated behind an env flag (e.g. `ALLOW_DEFAULT_OVERRIDE=false` by default) | Remove override now (rejected: breaks documented manual test workflow); treat cookie as authentication (rejected: it provides no proof of identity); leave undocumented (rejected: guessable backdoor if deployed publicly) |
| 2026-08-12 | SCOUT-014 follow-up: `ALLOW_DEFAULT_OVERRIDE` env gate closes 2026-07-31 default-cookie gap | SCOUT-014 re-confirmed override was still unconditional; SCOUT-017 deploy hardening depends on this | `api/session.py`: `allow_default_override()` reads `ALLOW_DEFAULT_OVERRIDE` (truthy: `1`/`true`/`yes`/`on`; default unset/false rejects `default`). Rejected override issues fresh `sess_<uuid4>` cookie (no error). Documented in `.env.example` + README; privacy panel updated. Tests: `tests/test_session_default_override.py`; existing suite sets `ALLOW_DEFAULT_OVERRIDE=true` in `conftest` autouse for legacy wardrobe API tests | Remove override entirely (rejected: 2026-07-31 decision); silent ignore without new session (rejected: acceptance criteria) |
| 2026-08-19 | SCOUT-016: GitHub Actions CI runs pytest on push and pull_request | Backlog M5: test suite should run automatically on every push/PR; no deploy automation in scope | **`.github/workflows/tests.yml`:** checkout, Python **3.12** (matches local `.venv`), `pip install -r requirements.txt`, `pytest -q`; fails job on any test failure. README status badge points at `ecekilicaslan15/stylescout-ai-agent` workflow. No matrix, no deploy step | Matrix across Python versions (rejected: minimal scope); Docker-based CI (rejected: unnecessary for unit tests) |
| 2026-08-19 | SCOUT-015 follow-up: suggested-item serialization crash + ai_inspiration ownership mismatch (production fixes) | Eval harness surfaced two blocking bugs: Mode 2/3 HTTP 500 when suggested catalogue items hit `read_item_id()` without a wardrobe row; Mode 3 mislabeled owned items because wardrobe identity set was id-only while candidates used name keys with mismatched category aliases (`bottom` vs `bottoms`) | **Bug A:** `serialize_outfit_item` assigns stable `sug_*` synthetic ids (via `synthetic_suggested_item_id`) for `source=suggested` items without a persisted id; `serialize_wardrobe_item`/`read_item_id()` unchanged for real wardrobe CRUD. Provenance override on name-merge only when agent already marked `source=wardrobe, owned=true` (avoids re-labeling explicit Mode 2 suggested slots). `create_outfit` uses `service._repository` so agent and serializer share one wardrobe snapshot. **Bug B:** Shared helpers in `wardrobe/normalization.py` — `wardrobe_identity_keys()` registers both id and canonical name+category keys per row; `find_matching_wardrobe_item()` + updated `resolve_inspiration_ownership()` merge persisted wardrobe id/metadata on match. DEFAULT_ITEMS categories (`top`, `bottom`, …) are valid `CATEGORY_ALIASES` per SCOUT-003. Tests: `tests/test_suggested_serialization_and_ownership.py` (empty-wardrobe API 200 + persisted-id ownership). Eval baseline post-fix: schema/provenance/cap 100%. **Still open:** search spec honors preferences 0.0% (separate SCOUT-008/009 follow-up, not shared root cause). | Synthetic ids for all outfit items including wardrobe rows (rejected: breaks `itm_*` guarantee); always set wardrobe provenance on serialize name-match (rejected: breaks Mode 2 explicit suggested items that share a name with wardrobe) |
| 2026-08-19 | SCOUT-015 follow-up: httpx2 test dependency + eval failure root-cause (no production fix) | Post-SCOUT-015: pytest collection failed on fresh envs with `RuntimeError: starlette.testclient requires httpx2`; eval report low pass rates traced | **Dependency:** `requirements.txt` adds `httpx2>=2.0.0` (Starlette TestClient prefers httpx2; plain httpx deprecated; RuntimeError when neither installed). **Eval root-cause (Part 2):** (1) 16× `Wardrobe item is missing a persisted id` on Mode 2/3 empty/partial fixtures — **production bug**, not harness wardrobe construction: `serialize_outfit_item` → `serialize_wardrobe_item` → `read_item_id()` requires id on **suggested** catalogue items with no wardrobe name match; partial harness item `PARTIAL_TOP` already has `id: eval_partial_top`. (2) `ai_full_casual` / `ai_full_edgy` — **production bug** in `resolve_inspiration_ownership()`: wardrobe rows use id-based `wardrobe_item_key` while DEFAULT_ITEMS use name+category keys, so Blue Jeans/White Sneakers never marked owned at generation; `serialize_outfit_item` name-merge adds wardrobe id but leaves `source=suggested, owned=false`, and validator correctly flags mismatch. **Not fixed this pass** (follow-up ticket). Fresh `eval/report.md` regenerated; metrics unchanged vs contaminated baseline (expected). | Patch production serialize/ownership in SCOUT-015 follow-up (deferred); fake ids in eval fixtures (rejected: would mask production bug) |
| 2026-07-31 | Free-text inline edit: expanded keyword matching, proper HTTP errors on failure | SCOUT inline-edit ticket: board ⟲ sent fixed elegant/casual strings; agent returned HTTP 200 + `success:false` for failures; no test that other outfit items stay unchanged | Keyword rules live in `agents/inline_edit_config.py` (comfort, formality, color, weather); InlineEditAgent returns `success:false` with error codes mapped to 422/500 in API; response includes full `outfit` with only target slot changed | Add LLM to InlineEditAgent (rejected: out of scope, AGENTS.md); keep silent 200 failures (rejected: frontend could not distinguish errors) |
| 2026-07-31 | Shopping deep-links via ShoppingService + VintedDeepLinkProvider | SCOUT-010: Mode 2 suggested items had no marketplace path; ShoppingAgent was a stub; AGENTS.md requires shopping as a deterministic service with no fake product/price data | `SearchSpec` built from outfit item fields; `services/shopping_service.py` composes public Vinted catalog URLs (`search_text` only); `shopping_link` attached in POST /api/outfits for Mode 2/3 suggested items only; ShoppingAgent remains a thin wrapper for orchestrator compatibility | Scrape/API integration (rejected: ToS/legal); fake catalog with prices (rejected: DECISIONS 2026-07-21); remove ShoppingAgent from registry now (rejected: separate refactor) |
| 2026-07-31 | Seed baseline reconciled with live default wardrobe (29 items, existing ids/metadata/Pinterest URLs preserved, session copies excluded from committed seed) | Squash commit shipped a stale 28-item metadata-less `wardrobe.json`; clean-checkout pytest failed on `read_item_id()` while live disk had SCOUT-006/007 ids, Pinterest URLs, and legitimate duplicate Black Jeans (`itm_f533acb5`) | Committed seed now mirrors live `user_id=default` rows only (29 items); all existing ids/timestamps/image URLs copied verbatim; `sess_*` session copies remain runtime-only via idempotent seed (SCOUT-007); Casual Shoulder Bag name kept per live-data policy | Re-hash ids from name/category (rejected: breaks restored `itm_f533acb5` and saved-outfit references); commit all 116 live rows including sessions (rejected: pollutes seed, duplicates seed logic); revert to old Unsplash Crossbody Bag baseline (rejected: contradicts live wardrobe policy) |
| 2026-07-31 | Correction: SCOUT-002 'single validation gate implemented' entry overstated actual committed scope | Only a partial Mode 1 referential validator was committed; schema/coherence layers, Mode 2 cap, Mode 3 ownership resolver, repair/fallback wiring, and planner mode contract were never committed; orchestrator's `mode=` call was incompatible with planner since 8183fba | Stopgap passthrough signature fix on `plan_user_request`/`Planner.plan` (accept `mode`, do not apply policy yet); full mode policy and validation gate deferred to a dedicated follow-up ticket because work spans three separable concerns (planner mode contract, full validation gate, SQLite alignment) | Force-commit large WIP now (rejected: unreviewable size, three mixed concerns); revert orchestrator `mode=` call (rejected: delays future mode contract work) |
| 2026-08-10 | SCOUT-002 follow-up sub-task A: planner mode contract + generation-time mode policy (not full validation gate) | Stopgap `mode=` passthrough left orchestrator calling planner with an ignored parameter; generation behavior lived only in `StylistAgent` mode branches without `Plan` policy flags | `Plan.apply_styling_mode()` sets `allow_external`/`wardrobe_optional`; rule/LLM planners and `plan_user_request` apply it; `generate_outfit` composes from plan flags (my_wardrobe = wardrobe-only, no DEFAULT_ITEMS; wardrobe_plus_ai = max 2 suggested at compose time; ai_inspiration = catalogue without wardrobe context, then public `resolve_inspiration_ownership()`); tests in `test_mode_contract.py` plus existing per-mode suites | Mark SCOUT-002 closed (rejected: sub-task B still needed); implement full schema/coherence/repair/fallback gate in this pass (rejected: sub-task B); wire gate to inline-edit (rejected: known deferred gap) |
| 2026-08-10 | Known gap (SCOUT-002 sub-task B pending): full validation gate not yet wired | Sub-task A enforces mode policy at generation time only; `OutfitValidator.validate()` remains partial (Mode 1 referential only; Mode 2/3 no-ops); no schema/coherence checks, repair-then-fallback, or post-generation gate call on POST /api/outfits | Deferred deliberately to sub-task B so mode contract lands reviewably first | Implement gate in sub-task A (rejected: scope); leave inline-edit ungated (accepted: existing DECISIONS row stands) |
| 2026-08-10 | Known gap (SCOUT-002 sub-task C pending): SQLite wardrobe alignment for source/owned | JSON and SQLite backends may diverge on provenance column persistence; not addressed in sub-task A | Deferred to sub-task C alongside any repository schema alignment work | Fix SQLite in sub-task A (rejected: separate concern) |
| 2026-08-10 | SCOUT-002 follow-up sub-task B: full validation gate wired on POST /api/outfits | Sub-task A enforced mode at compose time only; `OutfitValidator.validate()` was partial and never called in production | Extended `services/outfit_validator.py` (not a new file): Pydantic schema via `models/outfit.py`; coherence (duplicate slots + frontend SLOTS missing-slot documentation); Mode 2 suggested cap backstop; Mode 3 provenance checks; one deterministic repair pass then MY_WARDROBE composer fallback with user-visible reason; gate invoked in `api/main.py` after `run_fashion_agent` | New `services/outfit_validation.py` (rejected: duplicate module); wire gate into inline-edit (rejected: documented deferred gap) |
| 2026-08-10 | SCOUT-002 remains open pending sub-task C (SQLite source/owned alignment) | Sub-tasks A (mode contract) and B (validation gate on POST /api/outfits) are committed; inline-edit path still ungated; SQLite/JSON provenance persistence parity not verified | Close SCOUT-002 only after sub-task C lands and inline-edit gap is either accepted or addressed | Mark SCOUT-002 fully closed now (rejected: sub-task C still pending) |
| 2026-08-10 | SCOUT-002 sub-task B verification pass: gate backstop + repair re-check confirmed in code; tests tightened | Prior `test_validation_gate.py` exercised `collect_errors`/`_repair_outfit` helpers and one fallback test patched `_repair_outfit` to no-op; no test called `validate()`/`validate_and_finalize()` with a raw 3-suggested fixture independent of compose | Verified `validate_and_finalize` already re-runs full `collect_errors` on repaired output (lines 158–163) — no code change needed; added gate-entry tests: `validate()` rejects 3 suggested raw dict; `validate_and_finalize()` trims to ≤2 without compose; natural schema-invalid fixture proves repair re-check → MY_WARDROBE fallback without patching repair | Mark SCOUT-002 closed (rejected: sub-task C + inline-edit gap still open) |
| 2026-08-10 | Correction: SCOUT-008/009 were silently collapsed into SCOUT-010's item-only SearchSpec scope without an ADR | SCOUT-008 (shopping preference profile) and SCOUT-009 (structured search spec with preferences) were backlog tickets; committed code only had `SearchSpec {name, category, color, style}` from the outfit item and Vinted `search_text` deep-links (SCOUT-010). `memory/memory_store.py` holds style memory for generation, not shopping prefs. `AgentContext` lacked the AGENTS.md `preferences` field | Document honestly so future work does not assume profile/search-spec tickets were done; SCOUT-008/009 remain open beyond the partial scope closed in the next row | Mark SCOUT-008/009 fully closed retroactively (rejected: only item-only spec existed) |
| 2026-08-10 | SCOUT-008/009 partial: shopping preferences (`max_price`, `size` only) + preference-aware SearchSpec/deep-links | Minimal real profile needed for marketplace links without inventing Vinted structured price/size URL params (repo has no verified param names; SCOUT-010 uses `search_text` only) | `services/preference_service.py` JSON store keyed by session `user_id` (same cookie as SCOUT-007); GET/POST `/api/preferences`; optional `max_price`/`size` on `SearchSpec`; `build_search_spec(item, preferences=None)`; size/budget folded into `search_text` as `size {size}` and `under {max_price}` (known limitation); `AgentContext.preferences` added; skippable 2-field frontend form after successful outfit generation. **Deferred (explicit):** `currency`, `country`, `second_hand`, `exclude_brands` — need marketplace param research and/or UI scope; models allow future fields without breaking change | Build full 6-field profile now (rejected: scope); invent Vinted price/size URL params without evidence (rejected); mark SCOUT-008/009 fully closed (rejected: four fields still open follow-up) |
| 2026-08-10 | SCOUT-011: collapsed orchestrator agent inflation to exactly StylistAgent + InlineEditAgent | AGENTS.md hard rule: only two orchestrator-registered agents; AUDIT (2026-07-21) flagged five deterministic no-LLM classes wrongly registered as agents | **Removed:** `MemoryAgent` (orchestrator/API call `memory.memory_manager.update_memory_from_plan` directly), `ShoppingAgent` (shopping already in `services/shopping_service.py` + `api/main._attach_shopping_links`; orchestrator `shopping_request` stub preserved inline), `SewingAgent`/`TrendAgent` deleted (stub messages preserved inline for unreachable planner intents). **Renamed/re-homed:** `WardrobeAgent` scoring/matching → `services/wardrobe_matching_service.WardrobeMatchingService` (not merged into `wardrobe/wardrobe_service.py` — that class is CRUD/repository access only; different responsibility, naming collision avoided). InlineEditAgent now injects `WardrobeMatchingService`. Registry invariant tested in `tests/test_orchestrator_registry.py` | Merge matching into `WardrobeService` (rejected: CRUD vs scoring are distinct); keep ShoppingAgent wrapper (rejected: redundant after SCOUT-010) |
| 2026-08-10 | SCOUT-012: removed legacy Streamlit surface and trimmed dependencies | `app.py` was a second UI entry point (Streamlit) duplicating the FastAPI orchestrator path; AUDIT flagged 871MB Docker image bloat from Streamlit transitive deps | **Deleted:** `app.py`, `test_deneme.py` (one-line Streamlit smoke — no purpose without Streamlit). **Moved:** `test_rag.py` → `scripts/test_rag.py`, `manual_llm_check.py` → `scripts/manual_llm_check.py`. **requirements.txt:** removed `streamlit` only; retained `fastapi`, `uvicorn[standard]`, `pytest`, `python-dotenv`, `requests` (used by `services/llm_client.py` in services/ tree). Grep confirmed no `import streamlit` outside deleted files; pydeck/pyarrow/altair/pandas/numpy were never pinned — only Streamlit transitive installs. `app.py` orchestrator calls (`run_fashion_agent`, `run_inline_edit`) still matched SCOUT-011 signatures at deletion time (informational — file removed, not fixed) | Keep Streamlit as alternate UI (rejected: AGENTS.md vanilla JS + FastAPI is the entry point) |
| 2026-08-10 | SCOUT-013: grounded outfit explanation panel from validation gate satisfied constraints | Backlog M5: explanations must cite real checks, not adjectives; AGENTS.md forbids numeric scores in user copy | Extended `OutfitValidator.validate_and_finalize()` with `collect_satisfied_constraints()` + `explanation` list built by `build_outfit_explanation()` in `services/stylist_notes_builder.py`; attached top-level `explanation` on POST `/api/outfits`; simple panel in `frontend/index.html`. **Groundable today:** schema, duplicate-slot coherence, documented missing slots, mode provenance (MY_WARDROBE / WARDROBE_PLUS_AI cap / AI_INSPIRATION ownership). **Deferred (no validator check yet):** occasion/formality phrasing, color harmony, season — `Plan.event/style/city/date` exist but are not validated by the gate, so explanation does not claim them. `stylist_notes` (RAG fabric snippets) remains separate from `explanation`; pipeline trace labels (SCOUT-004) remain WHO-ran, not WHY-chosen. **Follow-up:** inline-edit swap clears `#explanationPanel` because the gate does not run on that path — symptom of the already-documented inline-edit gate gap, not a new issue | LLM-written explanation prose (rejected); occasion/color/season templates without checks (rejected: would be theatre) |
| 2026-08-12 | SCOUT-014: transparency pass — shared owned/suggested badges, search-labeled marketplace links, fallback/repair notice banner, honest privacy panel | Depends on SCOUT-010 (shopping links) and SCOUT-013 (explanation panel); backlog M5 disclosure | **`frontend/item-provenance.js`:** single `ItemProvenance` module for `provenanceBadgeHtml`, `shoppingLinkHtml` (`↗ Search on Vinted`), `validationNoticeHtml`, `wardrobeDisplayItem`; used by wardrobe grid, detail panel, and outfit board in `index.html`. Fallback/repair reasons (`FALLBACK_REASON`, `REPAIR_REASON_SUFFIX`) render in `#validationNotice` banner and are excluded from `#whyText` / duplicate explanation bullets. Static **Data & privacy** dialog discloses session-scoped wardrobe/saved outfits/preferences vs **global** `memory/memory_store.json` (re-verified: still single file, not per-user); documents `stylescout_session=default` override (gated in follow-up row same day); no saved-outfit or style-memory delete UI. **Deployment audit:** no Render/live URL config in repo — cannot confirm public production from codebase alone. Structural tests in `tests/test_transparency_ui.py` | Cookie consent / legal review (out of scope); overclaim isolation while default override remains unconditional (rejected — closed by SCOUT-014 follow-up same day); wire inline-edit validation gate (deferred) |
